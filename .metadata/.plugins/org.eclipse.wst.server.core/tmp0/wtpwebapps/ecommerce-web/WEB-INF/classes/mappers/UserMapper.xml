<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
         "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yourname.ecommerce.mapper.UserMapper">

  <!-- 1) 회원 삽입 -->
  <insert id="insertUser" parameterType="com.yourname.ecommerce.domain.User">
    INSERT INTO users (username, email, password)
    VALUES (#{username}, #{email}, #{password})
  </insert>

  <!-- 2) 아이디로 조회 -->
  <select id="findByUsername"
          resultType="com.yourname.ecommerce.domain.User">
    SELECT id, username, email, password
      FROM users
     WHERE username = #{username}
  </select>

</mapper>
